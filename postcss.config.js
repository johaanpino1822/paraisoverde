module.exports = {
    plugins: {
      tailwindcss: {}, // Asegúrate de que Tailwind esté configurado como un plugin de PostCSS
      autoprefixer: {}, // Agrega prefijos para compatibilidad con navegadores
    },
  };